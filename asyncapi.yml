asyncapi: 2.0.0
info:
  title: Test API with GitHub Reference
  version: 1.0.0
  description: Test API that references a GitHub URL

servers:
  test:
    url: test.mosquitto.org
    protdocol: mqtt

channels:
  user/signedup:
    publish:
      message:
        $ref: 'https://github.com/AayushSaini101/AayushSaini101/blob/main/user-signedup.yaml'
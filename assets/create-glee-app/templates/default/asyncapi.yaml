asyncapi: 3.0.0
info:
  title: 'Hello, Glee!'
  version: 0.1.0
servers:
  websockets:
    host: '0.0.0.0:3000'
    protocol: ws
channels:
  hello:
    address: hello
    messages:
      onHelloMessage:
        $ref: '#/components/messages/hello'
      subscribeMessage:
        $ref: '#/components/messages/hello'
operations:
  onHello:
    action: receive
    channel:
      $ref: '#/channels/hello'
    messages:
      - $ref: '#/components/messages/hello'
    reply:
      channel:
        $ref: '#/channels/hello'
      messages:
        - $ref: '#/components/messages/hello'
components:
  messages:
    hello:
      payload:
        type: string
